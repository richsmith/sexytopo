<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:keepScreenOn="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/panelBackground">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="@color/panelBackground"
                app:title="@string/title_activity_table"
                app:titleTextColor="@color/white" />

        </com.google.android.material.appbar.AppBarLayout>

        <LinearLayout
            android:id="@+id/tableContentLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@color/lightBackground">

            <!-- Header Row -->
            <LinearLayout
                android:id="@+id/HeaderContainer"
                android:layout_height="wrap_content"
                android:layout_width="match_parent"
                android:orientation="horizontal"
                style="@style/HeaderRow">
                <!-- Fixed Columns -->
                <TextView android:id="@+id/headerFrom" style="@style/HeaderText" android:layout_width="wrap_content" android:text="@string/table_head_from"/>
                <TextView android:id="@+id/headerTo" style="@style/HeaderText" android:layout_width="wrap_content" android:text="@string/table_head_to"/>
                <!-- Scrollable Columns -->
                <HorizontalScrollView
                    android:id="@+id/header_scroll_view"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:scrollbars="none">
                    <TableRow
                        style="@style/HeaderRow"
                        android:layout_width="wrap_content"
                        android:layout_height="match_parent">
                        <TextView style="@style/HeaderNum" android:layout_width="wrap_content" android:minWidth="70dp" android:text="@string/table_head_distance"/>
                        <TextView style="@style/HeaderNum" android:layout_width="wrap_content" android:minWidth="70dp" android:text="@string/table_head_azimuth"/>
                        <TextView style="@style/HeaderNum" android:layout_width="wrap_content" android:minWidth="70dp" android:text="@string/table_head_elevation"/>
                        <TextView android:id="@+id/headerComment" style="@style/HeaderComment" android:layout_width="wrap_content" android:minWidth="200dp" android:text="@string/table_head_comment"/>
                    </TableRow>
                </HorizontalScrollView>
            </LinearLayout>

            <!-- RecyclerView for scrollable content -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/tableRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"/>

        </LinearLayout>

    </LinearLayout>

    <!-- Mini FAB for Add Splay (positioned above primary FAB) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddSplay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:contentDescription="@string/manual_add_splay_title"
        app:fabSize="mini"
        app:srcCompat="@drawable/add_splay" />

    <!-- Primary FAB for Add Station -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAddStation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:contentDescription="@string/manual_add_station_title"
        app:srcCompat="@drawable/add" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
